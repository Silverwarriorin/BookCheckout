<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to "Edit this book", edit_book_path(@book) %> |
  <%= link_to "Back to books", "/full" %>

  <%= button_to "Remove from catalog", @book, method: :delete %>
</div>
